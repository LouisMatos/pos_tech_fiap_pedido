apiVersion: v1
kind: ServiceAccount
metadata:
  name: pedido-sa
  annotations:
    # CI substitui pelo ARN real (terraform output modules.irsa_pedido.role_arn do
    # ambiente dev) antes do kubectl apply - ver step "Set IRSA role arn" no workflow.
    eks.amazonaws.com/role-arn: "REPLACE_IRSA_ROLE_ARN"
