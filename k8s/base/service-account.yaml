apiVersion: v1
kind: ServiceAccount
metadata:
  name: pedido-sa
  # annotation eks.amazonaws.com/role-arn adicionada por overlay (role IRSA
  # difere por ambiente, ver k8s/overlays/<env>/service-account-patch.yaml)
